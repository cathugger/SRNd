      <div class="messageroot" id="${articlehash}">
        <a href="img/${imagelink}" class="image_link" target="_blank" title="${imagename}"><img src="thumbs/${thumblink}" class="image_root" />${play_button}</a>
        <a name="${articlehash}"></a><span class="topicline"><b>${subject}</b> ${author} <span class=published>${sent}</span>  ${root_quickreply} <a href="thread-${articlehash}.html">[${click_action}]</a> <span class="frontend">${thread_status}</span></span>
        ${signed}          <div class="stillevil">
          <input type="checkbox" value="${articlehash_full}" title="delete thread" class="purge_root" name="purge_root" id="purge${articlehash}">
          <label for="purge${articlehash}">delete thread</label>
          <input type="checkbox" value="${articlehash_full}" title="delete attachment" name="delete_a" id="del_a${articlehash}">
          <label for="del_a${articlehash}">delete attachment</label>
          <input type="checkbox" value="${articlehash_full}" title="Delete all i2p-user's posts" class="purge_root" name="purge_desthash" id="purge_desthash${articlehash}">
          <label for="purge_desthash${articlehash}">Delete all i2p-user's posts</label>
          <input type="checkbox" value="${articlehash_full}" title="sticky" name="sticky" id="sticky${articlehash}">
          <label for="sticky${articlehash}">${sticky_prefix}sticky thread</label>
          <input type="checkbox" value="${articlehash_full}" title="close" name="close" id="close${articlehash}">
          <label for="close${articlehash}">${close_action} thread</label>
        </div><br />
        <span class="frontend">[ ${frontend} ]</span>
        <br /><br />
        <span class="message_span">${message}</span>
        <br style="clear: both;"/>
      </div>
