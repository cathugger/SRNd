# empty lines or lines starting with # are ignored
# () == list
# [] == dict

uid_host=web.paste.geneticly.engineered.cat.girls.for.domestic.purposes
enforce_target=paste/%%sha1_message_uid_10%%.html
enforce_board=ano.paste

(required_fields)
name
email
subject
board
reply
comment
target

[defaults]
subject=no title
name=Anonymous
email=nobody@no.where

[custom]
hidden=false
language=auto
